{
  "name": "leet-leaderboard",
  "version": "0.1.0",
  "private": true,
  "description": "Self-updating LeetCode leaderboard for a friend group, deployed on Vercel + GitHub Actions.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "snapshot": "node scripts/fetch.mjs"
  }
}
